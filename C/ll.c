
#include <stdio.h>
#include <stdlib.h>  // used to call malloc function

struct node{
    int data;
    struct NODE *link;
};


NODE insertlast(first );

newnode = (newnode)malloc(sizeof(struct node)) 
newnode = ()

if(first ==null){
  printf("%d\n", node null);
}

void main(){
  NODE f1, f2,f3;
  f1= NULL ; f2=NULL;
  printf("list 1");
  f1= create list(f1);
  printf("list2 ");
  f2 =craetelist(f2);
  traverse(f1);
  traverse(f2);

  f3=conactenate(f1,f2);
  traverse(f3);


}


