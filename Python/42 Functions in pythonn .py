# A function is a block of code that performs a specific task whenever it is called. In bigger programs, where
# we have large amounts of code, it is advisable to create or use existing functions that make the program 
# flow organized and neat.

def multiplynumbers (a,b): # here we can define a function 
    multiplynumbers =a*b
    print(multiplynumbers)


a=8
b=9
multiplynumbers (a,b) # now we just have to write multiplynumbers to multiply them at any point in the code



a=46
b=90
multiplynumbers(a,b)



c=3
d=7
multiplynumbers(c,d)


def multiplynumber(a,b):
   pass # pass is uded to temporarily skip  a function

a=2
b=1
if(a>b):
 print("a is greater than b")

else:
   print("b is greater than a")


