let age=window.prompt("enter the number");

age=Number(age); // this funtion convert other data type into a number

num+=1;
// console.log(num); // by dfault it is string data type

// we have to change it to integeer data type

age=Number(age) // converts the data type to number(integer)


// now 



let x="pizza";
let y=23;
let z=True;

x=Number(x); //converts to integer
y=String(y); // converts to string
z=Boolean(z); // converts to boolean

console.log(x)
console.log(y)
console.log(z)