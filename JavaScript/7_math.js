//Used for doing maths

console.log(Math.PI) // pi
console.log(Math.E) // e (exponent)

// now with numbers

let x=3.26;
let y=2;
let z;

z=Math.round(x);
z=Math.floor(x);
z=Math.ciel(x);
z=Math.trunc(x); //removes any decimal value
z=Math.pow(x,y); 
z=Math.sqrt(x); 
z=Math.log(x); 
z=Math.sin(x); 
z=Math.cos(x); 
z=Math.tan(x); 
z=Math.abs(x); // gives only positive vaule
z=Math.sign(x); //gives sign of a number +ve/-ve

let a= 2;
let b= 9;
let c= 4;
let d= 7;

let max;

max=Math.max(a,b,c,d); //gives the max value
max=Math.min(a,b,c,d); //gives the min value

console.log(min);

